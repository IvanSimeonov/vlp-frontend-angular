import { WhiteSpaceNumberPipe } from './white-space-number.pipe';

describe('WhiteSpaceNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new WhiteSpaceNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
